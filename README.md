HTML and CSS -- working on forms --

html code
  diferent types of forms and inputs
css code
  diferent types of forms and inputs

  use of HTML: "fieldset", "legend", "Label", "button", "select", "type", "placeholder", ""required" (email field), "value", ... 
  "field cols and rows", example svg path use direct HTML

  use of:  
  - more than one index.css
  - element + element {}, --> elimina o uso no 1o elemento, só aplicando o estilo mos elementos adjacentes
          "fieldset + fieldset {
              margin-top: 3rem;
          }" --> desconsidera margin-top no primeiro fieldset
   
  - flex "grow" "shrink" "basis" --> for checkbox, avatar, icons and badges - usually followed by height with same value of basis to
  configure a square

  - many uses of "element {
                  &"

  - all:unset --> to clear any previous setup
  - &:hover
    &:focus
    &:focus-within
    &:has(:checked)
